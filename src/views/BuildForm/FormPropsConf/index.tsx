import React from 'react'
import styles from '../index.module.less'
import {FormPropsConfProps, FormPropsConfState} from "./interface";

class FormPropsConf extends React.Component<FormPropsConfProps, FormPropsConfState> {

  render() {
    return (
      <section className={styles.FormPropsConf}>
      </section>
    )
  }
}

export default FormPropsConf;
